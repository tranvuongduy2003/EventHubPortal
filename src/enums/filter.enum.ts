export enum EPageOrder {
  ASC = "ASC",
  DESC = "DESC",
}

export enum EEventType {
  ALL = "ALL",
  UPCOMING = "UPCOMING",
  OPENING = "OPENING",
  CLOSED = "CLOSED",
}

export enum EEventPrivacy {
  ALL = "ALL",
  PUBLIC = "PUBLIC",
  PRIVATE = "PRIVATE",
}
