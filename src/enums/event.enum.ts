export enum EEventStatus {
  OPENING = "OPENING",
  UPCOMING = "UPCOMING",
  CLOSED = "CLOSED",
}

export enum EEventCycleType {
  RECURRING = "RECURRING",
  SINGLE = "SINGLE",
}

export enum EEventPaymentType {
  PAID = "PAID",
  FREE = "FREE",
}
